Функция ПолучитьЗначениеСвойстваXDTO(ОбъектXDTO,ИмяСвойства)
	
	Если НЕ ОбъектXDTO.Свойства().Получить(ИмяСвойства) = Неопределено Тогда
		
		Возврат ОбъектXDTO[ИмяСвойства];
		
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции