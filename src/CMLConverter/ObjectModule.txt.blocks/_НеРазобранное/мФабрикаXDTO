Функция мФабрикаXDTO() Экспорт
	
	Результат = ЭДО_Кэш_Прочитать("мФабрикаXDTO");
	
	Если Результат = Неопределено Тогда
		
		ФайлСхемы = ПолучитьИмяВременногоФайла("xsd");
		ПолучитьМакет("СхемаCML").Записать(ФайлСхемы);
		Результат = СоздатьФабрикуXDTO(ФайлСхемы);
		УдалитьФайлы(ФайлСхемы);
		
		ЭДО_Кэш_Поместить("мФабрикаXDTO",Результат);
		
	КонецЕсли;
	
	Возврат Результат;
	
КонецФункции