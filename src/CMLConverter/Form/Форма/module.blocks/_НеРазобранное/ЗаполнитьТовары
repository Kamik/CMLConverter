Процедура ЗаполнитьТовары(НовОбъект,ТаблицаКаталога)
	
	_Каталог	= xmlСоздатьДочернийЭлементXDTO(НовОбъект,	"Каталог");
	_Товары		= xmlСоздатьДочернийЭлементXDTO(_Каталог,	"Товары");
	
	Сч=0;
	
	Для каждого СтрокаТовара Из ТаблицаКаталога Цикл
		
		Попытка
			
			НовыйТоварXDTO = ДобавитьЭлементСпискаXDTO(_Каталог.Товары.Товар,мФабрикаXDTO(),Ложь);
			НовыйТоварXDTO.Ид				= СтрокаТовара.Штрихкод;
			НовыйТоварXDTO.Штрихкод			= СтрокаТовара.Штрихкод;
			НовыйТоварXDTO.Наименование		= СтрокаТовара.Наименование;
			
			Если ЗначениеЗаполнено(СтрокаТовара.БазоваяЕдиница) Тогда
				
				xmlСоздатьДочернийЭлементXDTO(НовыйТоварXDTO,"БазоваяЕдиница");
				НовыйТоварXDTO.БазоваяЕдиница.Код	= СтрокаТовара.БазоваяЕдиница;
				
			КонецЕсли;
		
			Сч=Сч+1;
			
		Исключение
			
			_Каталог.Товары.Товар.Удалить(Сч);//хз что с ним делать
			
		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры